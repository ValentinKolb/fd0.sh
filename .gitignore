# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editors
*.swp
*.swo
*~
.vscode/
.idea/
*.iml

# Build artifacts
bin/
dist/
build/
target/
out/

# Go
*.test
*.out
vendor/
# Stray binaries from `go build .` at repo root
/fd0
/fd0-server
/fd0-witness

# Local environment
.env
.env.local
.envrc

# fd0 runtime data (in case anyone runs the client from the repo)
.fd0/

# Local-only working notes
TODO.md

# website/ — Bun + Hono + Solid (via @valentinkolb/ssr) homepage
website/node_modules/
website/.bun/
website/dist/
# Dev-mode Tailwind output (regenerated on every `bun run dev` boot).
# The production build writes its own copy into website/dist/public/.
website/public/styles.css
# Self-hosted Geist + Geist Mono woff2 subsets: copied by
# scripts/copy-fonts.ts from node_modules on every dev/prod build.
# Source of truth is @fontsource-variable/{geist,geist-mono}.
website/public/fonts/
